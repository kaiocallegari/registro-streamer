DISCORD_TOKEN=seu_token_aqui

# Em produção (ex: Railway), aponte para dentro do Volume persistente
# que você anexar ao serviço, para o banco sobreviver aos redeploys.
# Exemplo: DB_PATH=/data/metas.db
DB_PATH=metas.db
